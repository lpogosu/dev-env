# Intentionally empty: this variant measures what zsh itself costs to start on
# the machine under test, so that every other number in the benchmark can be
# read as "cost of the configuration" rather than "cost of running zsh".
